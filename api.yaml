runtime: nodejs

runtime_config:
  operating_system: 'ubuntu22'

service: api

env_variables:
  APP_RUNTIME_ENVIRONMENT: ['npm', 'start']

env: flex

service_account: aklapper@games-424800.iam.gserviceaccount.com

inbound_services:
  - warmup
entrypoint: 'dist/apis/games-api'

handlers:
  - url: /.*
    script: None
    secure: always
  - url: /.*
    script: None
    secure: always

manual_scaling:
  instances: 1

resources:
  cpu: 2
  memory_gb: 4
  disk_size_gb: 10
