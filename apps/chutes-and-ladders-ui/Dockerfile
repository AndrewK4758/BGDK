# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build chutes-and-ladders-ui`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t chutes-and-ladders-ui`.
FROM docker.io/node:22-alpine

ENV HOST=0.0.0.0
ENV PORT=4200

WORKDIR /app 

COPY . .

RUN npm install

RUN npx nx run chutes-and-ladders-ui:build:production

EXPOSE 4200
